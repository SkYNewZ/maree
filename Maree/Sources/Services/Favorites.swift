import SwiftUI

/// Placeholder so the species screen's toolbar compiles. Task 10 owns this file
/// and replaces this with the real favourites store and button.
struct FavoriteButton: View {
    let speciesId: Int

    var body: some View { EmptyView() }
}
