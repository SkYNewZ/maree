import SwiftUI

// Placeholders, replaced in a later task.
struct GroupView: View {
    let group: TaxonGroup
    var body: some View { Text(group.name) }
}

struct GroupSpeciesView: View {
    let group: TaxonGroup
    var body: some View { Text(group.name) }
}
